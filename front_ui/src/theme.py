"""화면 전체에서 쓰는 색·간격 토큰.

색을 직접 문자열로 쓰지 말고 여기 상수를 쓴다.
나중에 발표용으로 톤을 바꿀 때 이 파일만 고치면 된다.
"""

# ── 배경 / 표면 ──────────────────────────────────────────────
BG = "#0F1319"          # 앱 배경
SURFACE = "#171C24"     # 패널 배경
SURFACE_ALT = "#1E2530"  # 패널 안쪽 강조 영역
BORDER = "#2A323E"      # 패널 테두리
RAIL = "#131820"        # 좌측 네비게이션

# ── 글자 ────────────────────────────────────────────────────
TEXT = "#E3E8F0"        # 본문
TEXT_DIM = "#8A94A6"    # 보조 설명
TEXT_FAINT = "#5A6474"  # 비활성 단계, 미사용 항목

# ── 강조 ────────────────────────────────────────────────────
ACCENT = "#E0A458"      # 현재 진행 중인 것 (현재 단계, 선택된 탭)

# ── 상태 색 ─────────────────────────────────────────────────
STATUS = {
    "unknown": "#6B7688",    # 미확인 / 위치 불명
    "searching": "#4A90D9",  # 탐색 중
    "confirmed": "#3FB27F",  # 확인됨 / 발견
    "held": "#9B7FD4",       # 손에 있음
    "warning": "#E0A458",    # 경고
    "error": "#D9564A",      # 오류 / 실패
}

# 시스템 상태 색
SYSTEM_STATE = {
    "LOAD": TEXT_DIM,
    "IDLE": "#4A90D9",
    "RUN": "#3FB27F",
}

# ── 3D 지도 ─────────────────────────────────────────────────
ROBOT = "#E5493E"  # 로봇 위치 마커. STATUS["error"]와 비슷한 계열이지만 의미가 달라 분리

# ── 간격 ────────────────────────────────────────────────────
GAP = 8          # 패널 사이 간격
PAD = 14         # 패널 안쪽 여백
RADIUS = 6

# ── 글자 크기 ────────────────────────────────────────────────
SIZE_LABEL = 11   # 패널 제목
SIZE_BODY = 13
SIZE_VALUE = 15   # 값 강조
SIZE_BIG = 22     # 대상 물체 이름 등

MONO = "monospace"  # 좌표·시간·ID 처럼 자리수가 흔들리면 안 되는 값